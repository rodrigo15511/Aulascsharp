namespace Desafio3.enum
{
    public enum Estado
    {










        string endereco = Console.ReadLine();
        System.Console.WriteLine("Digite o Estado: (sigka, ex: SP, MT)");
        string estado = Console.ReadLine();

        if ("Enum.TryParse(estado, out Estado estado Enum))
        {
            
        }










    }
}